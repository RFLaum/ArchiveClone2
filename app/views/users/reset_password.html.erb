<%= simple_form_for @user,
  url: reset_pw_path(@user.name, @user.confirmation_hash),
  method: :post do |f| %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.button :submit, "Reset Password" %>
<% end %>
