<%= simple_form_for(@newspost) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :admin_name %>
    <%= f.input :title %>
    <%= f.input :content %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
