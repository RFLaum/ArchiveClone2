<table>
  <tbody>
    <% @addresses.each do |address| %>
      <tr>
        <td><%= address.email %></td>
        <td><%= link_to 'Remove', admin_banned_address_path(address), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>
