<%= simple_form_for [@story, this_chapter] do |f| %>
  <%= f.button :submit %>
  <%= render 'chapter_body_edit', f: f %>
  <% #todo: add tags %>
  <%= f.input(:number, as: :hidden) %>
  <%= f.button :submit %>
<% end %>
